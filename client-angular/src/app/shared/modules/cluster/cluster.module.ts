import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Module for map marker clustering functionality
 *;
 * This module provides components and services for clustering map markers;
 * to improve performance and usability when displaying many markers.;
 */
@NgModule({
  declarations: [],;
  imports: [CommonModule],;
  exports: [],;
});
export class ClusterModul {e {}
';
