<div *ngIf="loading">Loading...</div>
<div *ngIf="error">{{ error }}</div>
<div *ngIf="ads.length">
  <div>
    <h3>{{ getCurrentAd()?.title }}</h3>
    <p>{{ getCurrentAd()?.description }}</p>
    <button (click)="swipeLeft()">Swipe Left</button>
    <button (click)="swipeRight()">Swipe Right</button>
    <button (click)="viewAdDetails(getCurrentAd()?._id)">View Details</button>
    <button (click)="searchNearby()">Search Nearby</button>
  </div>
</div>
